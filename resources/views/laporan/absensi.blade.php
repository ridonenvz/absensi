@extends('layouts.app')
@section('title', 'Laporan Absensi')
@section('content')
<div class="page-header"><h1 class="page-title">Laporan Absensi</h1><p class="page-subtitle">Rekap kehadiran pegawai per bulan.</p></div>
<div class="card mb-3"><div class="card-body"><form class="form-inline" method="GET" action="{{ route('laporan.absensi') }}"><label class="mr-2">Bulan</label><input type="number" min="1" max="12" name="bulan" value="{{ $bulan }}" class="form-control mr-2"><label class="mr-2">Tahun</label><input type="number" name="tahun" value="{{ $tahun }}" class="form-control mr-2"><button class="btn btn-danger mr-2">Tampilkan</button><a href="{{ route('laporan.absensi.pdf', ['bulan'=>$bulan,'tahun'=>$tahun]) }}" class="btn btn-outline-danger"><i class="fas fa-file-pdf mr-1"></i>PDF</a><a href="{{ route('laporan.absensi.excel', ['bulan'=>$bulan,'tahun'=>$tahun]) }}" class="btn btn-outline-danger ml-2"><i class="fas fa-file-excel mr-1"></i>Excel</a></form></div></div>
<div class="card"><div class="card-body p-0"><table class="table mb-0"><thead><tr><th>Tanggal</th><th>Pegawai</th><th>Masuk</th><th>Pulang</th><th>Telat</th><th>Kompensasi</th><th>Status</th></tr></thead><tbody>@forelse($data as $row)<tr><td>{{ $row->tanggal }}</td><td>{{ $row->pegawai->nama ?? '-' }}</td><td>{{ $row->jam_masuk ?? '-' }}</td><td>{{ $row->jam_pulang ?? '-' }}</td><td>{{ $row->menit_telat }} menit</td><td>{{ $row->menit_kompensasi }} menit</td><td><span class="badge badge-light">{{ ucfirst($row->status) }}</span></td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-4">Data tidak ditemukan.</td></tr>@endforelse</tbody></table></div></div>
@endsection
