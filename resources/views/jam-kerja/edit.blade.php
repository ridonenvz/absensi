@extends('layouts.app')
@section('title', 'Edit Jam Kerja')
@section('content')
<div class="page-header"><h1 class="page-title">Edit Jam Kerja</h1><p class="page-subtitle">Perbarui jadwal kerja.</p></div>
<div class="card"><div class="card-body"><form method="POST" action="{{ route('jam-kerja.update',$jamKerja->id) }}">@csrf @method('PUT')<div class="row"><div class="col-md-4 form-group"><label>Hari</label><select name="hari" class="custom-select">@foreach($hari as $h)<option value="{{ $h }}" {{ $jamKerja->hari === $h ? 'selected' : '' }}>{{ $h }}</option>@endforeach</select></div><div class="col-md-4 form-group"><label>Jam Masuk</label><input type="time" name="jam_masuk" class="form-control" value="{{ old('jam_masuk', $jamKerja->jam_masuk ? substr($jamKerja->jam_masuk,0,5) : '') }}"></div><div class="col-md-4 form-group"><label>Jam Pulang</label><input type="time" name="jam_pulang" class="form-control" value="{{ old('jam_pulang', $jamKerja->jam_pulang ? substr($jamKerja->jam_pulang,0,5) : '') }}"></div></div><div class="custom-control custom-switch mb-3"><input type="checkbox" name="is_wfh" value="1" class="custom-control-input" id="is_wfh" {{ $jamKerja->is_wfh ? 'checked' : '' }}><label class="custom-control-label" for="is_wfh">WFH</label></div><div class="d-flex justify-content-between"><a href="{{ route('jam-kerja.index') }}" class="btn btn-outline-secondary">Kembali</a><button class="btn btn-danger">Simpan</button></div></form></div></div>
@endsection
